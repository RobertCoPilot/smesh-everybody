rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development-only rules. Tighten before any public production deploy.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
